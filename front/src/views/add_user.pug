html
    head
        title add a user
    body
        h1 add a user
        form(
            name="myform"
            id="login_form"
            action="/add_user"
            method="post"
        )
            input(
                type="text"
                name="username"
                placeholder="username"
            )
            input(
                type="submit"
                value="submit"
            )
